<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ServiciosRest.MVVM.Views.UsuariosView"
             Title="UsuariosView">
    <VerticalStackLayout>
        <Button Command="{Binding GetAllUsersCommand}" Text="Obtener Usuarios"/>
        <Button Command="{Binding GetSingleUserCommand}" Text="Obtener un sólo Usuarios"/>
        <Entry/>
        <Entry/>
        <Entry/>
        <Button Command="{Binding AddUser}" Text="Subir Usuario"/>
    </VerticalStackLayout>
</ContentPage>